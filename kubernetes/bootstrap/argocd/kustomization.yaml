apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # The upstream base that install.yaml is generated from, pinned by tag.
  # Renovate bumps the ref; the upgrade diff is this one line.
  - github.com/argoproj/argo-cd/manifests/cluster-install?ref=v3.4.4
