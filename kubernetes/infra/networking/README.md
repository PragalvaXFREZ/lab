# networking

The cluster's networking layer: the CNI and the L2/L3 plumbing that everything else depends on. This is where Cilium, MetalLB, and the Gateway API resources and routes live.

Lands under its own follow up issue, alongside ingress and TLS.
