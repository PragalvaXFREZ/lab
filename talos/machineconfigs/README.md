# machineconfigs

Per node Talos machine configuration, one file per node. Applied with `talosctl apply-config`. Treated as reviewable, version controlled state, never edited live on a node without the change landing here first.
