## Gateway API CRDs

Gateway API CRDs are installed cluster-wide from upstream
(kubernetes-sigs/gateway-api).

Version: v1.1.0

This provides:
- GatewayClass
- Gateway
- HTTPRoute
- ReferenceGrant

Controllers are installed separately.
