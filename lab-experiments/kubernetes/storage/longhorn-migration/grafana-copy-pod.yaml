apiVersion: v1
kind: Pod
metadata:
  name: grafana-storage-copy
  namespace: monitoring
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: talos-opt-7040
  containers:
    - name: copy
      image: alpine:3.22
      command:
        - sh
        - -c
        - sleep 1d
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          memory: 64Mi
      volumeMounts:
        - name: source
          mountPath: /source
          readOnly: true
        - name: destination
          mountPath: /destination
  volumes:
    - name: source
      persistentVolumeClaim:
        claimName: kps-grafana
    - name: destination
      persistentVolumeClaim:
        claimName: kps-grafana-longhorn
